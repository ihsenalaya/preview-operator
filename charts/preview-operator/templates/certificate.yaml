{{- if and .Values.webhook.enabled .Values.certManager.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "preview-operator.fullname" . }}-webhook-cert
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "preview-operator.labels" . | nindent 4 }}
spec:
  dnsNames:
  - {{ include "preview-operator.webhookServiceName" . }}.{{ .Release.Namespace }}.svc
  - {{ include "preview-operator.webhookServiceName" . }}.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: {{ include "preview-operator.issuerName" . }}
  secretName: {{ include "preview-operator.webhookCertSecret" . }}
{{- end }}
