{{- if .Values.webhook.enabled }}
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: {{ include "preview-operator.fullname" . }}-validating-webhook
  labels:
    {{- include "preview-operator.labels" . | nindent 4 }}
  {{- if .Values.certManager.enabled }}
  annotations:
    cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ include "preview-operator.fullname" . }}-webhook-cert
  {{- end }}
webhooks:
- admissionReviewVersions: [v1]
  clientConfig:
    service:
      name: {{ include "preview-operator.webhookServiceName" . }}
      namespace: {{ .Release.Namespace }}
      path: /validate-platform-company-io-v1alpha1-preview
  failurePolicy: Fail
  name: vpreview.kb.io
  rules:
  - apiGroups: [platform.company.io]
    apiVersions: [v1alpha1]
    operations: [CREATE, UPDATE]
    resources: [previews]
  sideEffects: None
{{- end }}
