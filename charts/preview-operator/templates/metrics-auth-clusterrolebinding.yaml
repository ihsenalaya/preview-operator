apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "preview-operator.fullname" . }}-metrics-auth-rolebinding
  labels:
    {{- include "preview-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "preview-operator.fullname" . }}-metrics-auth-role
subjects:
- kind: ServiceAccount
  name: {{ include "preview-operator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
