# Phase 5a freeze

This file pins everything that produced the matrix attempt 7-bis bundles
on AKS, so that the Phase-5b/c/d cross-LLM runs can be derived without
ambiguity. **Do not edit this file** after the freeze.

Per `execution-plan.md §4`, any edit to the `do-not-edit` set below
discards the LLM-B numbers and forces a fresh Phase-5a snapshot.

---

## Freeze metadata

- **Tag:** `frozen-for-llm-b-20260523`
- **Commit SHA:** `2e81a648c3244c276dc3d58821a01e61d536901e`
- **Commit subject:** `docs(experimentations): matrix attempt 7-bis complete — F1-F10 = 100/100 captured`
- **Frozen at (UTC):** 2026-05-23 11:59 UTC (~immediately after the matrix `=== Done` line at 11:58:51)
- **Branch:** `article/failure-provenance`

## Operator

- **Image (mutable tag used at deploy time):** `testagentdevops.azurecr.io/preview-operator:fp-fullsuite`
- **Image digest (pinned, source of truth):** `testagentdevops.azurecr.io/preview-operator@sha256:88cd767f41c07f0eb65d85ea0d95f543926d07839f373b099dbb1c1d591a3a30`
- **Pod observed at freeze:** `preview-operator-656b5dff76-xsnmx` in namespace `preview-operator-system`
- **Defect count fixed in this image:** 16 (defect #1 status-clobber → defect #15 ActiveDeadlineSeconds, plus the FullSuite-bypass under `failure-provenance.experiment/owned=true` label in commit `39231a5`)

## Cluster

- **Name:** `idp-preview-test`
- **Type:** AKS, 3× Standard_D4s_v3 (4 vCPU, 16 GiB RAM each), Kubernetes 1.34.7
- **Cluster type tag in CSV:** `aks`

## LLM-A (sole LLM live in Phase 5a)

- **Provider:** Azure OpenAI
- **Endpoint:** `https://preview-openai-idp.openai.azure.com/openai/deployments/gpt-4o-mini`
- **Model snapshot:** `gpt-4o-mini-2024-07-18`
- **Temperature:** 0
- **Rate limit:** ~30 K TPM (defect #2 retry/backoff in place since commit `5b43311`)

## Data

- **Results CSV:** `experiments/failure-provenance/results-matrix/results.csv` — 1500 data rows + 1 header
- **FailureReport bundles:** `experiments/failure-provenance/results-matrix/F[1-10]/r[1-10]/report.json` (100 files)
- **Per-rep diag/score artifacts:** `…/diag-{C1..C5}-{rule,llm}-{grounded,freeform}.json`
- **Backup snapshots:** `results-matrix-F1-F4-snapshot.csv`, `results-pre-rerun-F4.csv`, `results-matrix/F4-old-attempt7/`

## Captured volume

- **Cluster runs:** 100 / 100 (10 scenarios × 10 reps)
- **No-report rows:** 0
- **Result-CSV rows:** 1500 (100 runs × 5 evidence levels × 3 engine modes)
- **Errors:** 0

## Do-not-edit set (freeze policy from `execution-plan.md §4`)

Editing any of these files after this freeze invalidates the cross-LLM
comparison. Discard the LLM-B numbers and re-snapshot Phase 5a if any
edit is required:

- `experiments/failure-provenance/scenarios.yaml`
- `internal/evidence/*` (any collector, any aggregation)
- `api/v1alpha1/failurereport_types.go` (any CRD shape change)
- `experiments/failure-provenance/scoring-rubric.md`
- `internal/evidence/collectors.go`
- The operator's diagnostic prompt template (if changed, log the difference per row)

## Allowed edits (these are LLM-B-only changes, not shared with LLM-A bundles)

- `internal/diagnosis/together.go` — new file for the Llama-3.3 backend
- A CLI / env-var flag in `run-kind-experiments.sh` or `fp-diagnose` to select LLM-A vs LLM-B
- `experiments/failure-provenance/analysis/` — analysis scripts (do not touch the bundles)
- `docs/research/failure-provenance/EVALUATION-DRAFT.md` — write-up

## What this freeze enables

- Phase 5c (LLM-B offline replay) can be run cheaply against the 100 bundles
  without re-burning cluster time. Any LLM-B row will be tagged
  `frozen_commit_sha=2e81a648c3244c276dc3d58821a01e61d536901e` in the CSV
  per the augmented schema (task #18).
- Reproducibility: a third party can re-derive every figure in the
  Evaluation section from the 100 `report.json` files in
  `results-matrix/F[1-10]/r[1-10]/` using the published `fp-diagnose` and
  `fp-score` binaries at this commit.
- Honest cross-LLM claim: any difference between LLM-A and LLM-B numbers
  in the Evaluation section is provably from the LLM, not from a shifted
  operator or evidence collector, because the bundles are identical and
  pinned.
