---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: preview-extension
  namespace: preview-operator-system
  labels:
    app: preview-extension
spec:
  replicas: 1
  selector:
    matchLabels:
      app: preview-extension
  template:
    metadata:
      labels:
        app: preview-extension
    spec:
      serviceAccountName: preview-extension
      containers:
        - name: extension
          image: ghcr.io/ihsenalaya/preview-extension:0.12.6
          ports:
            - containerPort: 8090
              name: http
          env:
            - name: LISTEN_ADDR
              value: ":8090"
            - name: GITHUB_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: preview-extension-secret
                  key: webhook-secret
                  optional: true
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8090
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8090
            initialDelaySeconds: 3
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: preview-extension
  namespace: preview-operator-system
spec:
  selector:
    app: preview-extension
  ports:
    - port: 8090
      targetPort: 8090
      name: http
  type: ClusterIP
